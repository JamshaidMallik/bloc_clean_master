# bloc_clean_master

A fresh Flutter project.

The previous Clean Architecture and Riverpod sample implementation has been removed so the app can be rebuilt from scratch.

## Getting Started

Install dependencies:

```bash
flutter pub get
```

Run the app:

```bash
flutter run
```
